#!/usr/bin/env node

console.log('Clearing AsyncStorage is only possible from within the app runtime.');
console.log(
  'To clear AsyncStorage on a device, use a development tool or add a debug button in your app.'
);
console.log('This script is a placeholder for CI/dev environments.');
process.exit(0);
