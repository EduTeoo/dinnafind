#!/usr/bin/env node

console.log(
  'Clearing Redux Persist storage is only possible from within the app runtime or by deleting device files.'
);
console.log(
  'To clear Redux Persist on a device, use a development tool or add a debug button in your app.'
);
console.log('This script is a placeholder for CI/dev environments.');
process.exit(0);
